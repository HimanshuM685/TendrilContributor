{
  "name": "tendril-contributor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tendril contributor agent — rent out this machine's CPU/RAM/GPU as hardened, ephemeral SSH sandboxes",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "socket.io-client": "^4.7.5",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
